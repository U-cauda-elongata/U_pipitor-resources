[package]
name = "test-suite"
version = "0.0.0"
authors = ["Yu Onaga <ursus.cauda.elongata@gmail.com>"]
edition = "2018"
publish = false

[dependencies]
toml = "0.5"

[dependencies.pipitor]
version = "0.3.0-alpha.1"
default-features = false
features = ["rustls", "sqlite-bundled"]
